[package]
name = "keep-enclave"
version = "0.3.0"
edition = "2021"
rust-version = "1.89"
description = "AWS Nitro Enclave signer for Keep"
license = "MIT"

[dependencies]
keep-enclave-host = { path = "host" }

[features]
default = []
